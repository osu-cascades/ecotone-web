- provide(:title, "All Users")
%h1 All Users
%ul.indexing
  - @users.each do |user|
    %li
      = link_to user.name, user, title: "Details of #{user}"
      = "(admin)" if user.admin?
      = delete_user_link(current_user, user)